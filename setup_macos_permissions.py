#!/usr/bin/env python3
"""
Скрипт для открытия настроек доступности macOS
"""

import subprocess
import sys


def open_accessibility_settings():
    """Открыть настройки доступности"""
    print("🔐 Открытие настроек доступности macOS...")
    print()
    
    # Пробуем открыть через System Settings (macOS 13+)
    try:
        subprocess.run([
            "open",
            "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility"
        ], check=True)
        print("✅ Откройте 'Системные настройки' → 'Защита и безопасность' → 'Доступность'")
    except subprocess.CalledProcessError:
        # Альтернативный способ для старых версий
        subprocess.run([
            "open",
            "x-apple.systempreferences:com.apple.preference.universalaccess"
        ])
        print("✅ Откройте 'Системные настройки' → 'Универсальный доступ'")
    
    print()
    print("📋 Инструкция:")
    print("1. Найдите в списке слева ваш терминал или Python")
    print("2. Поставьте галочку рядом с ним")
    print("3. Если нет в списке — нажмите '+' и добавьте приложение")
    print("4. Перезапустите приложение после предоставления доступа")
    print()
    
    # Предложение открыть терминал
    print("💡 Также можно открыть через терминал:")
    print("   tccutil reset Accessibility")
    print("   (сбросит все разрешения для доступности)")


if __name__ == "__main__":
    open_accessibility_settings()
