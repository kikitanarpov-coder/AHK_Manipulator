# AHK Manipulator

Визуальный конструктор AHK-скриптов с task-доской для управления действиями на экране.

## 🎯 Возможности

- **Task-доска** — визуальное представление последовательности действий
- **Запись действий** — запись перемещений мыши и нажатий клавиш
- **Типы действий**:
  - 🖱 Клик мышью (левый, правый, средний)
  - ➡ Перемещение мыши
  - ⌨ Нажатие клавиш и горячих комбинаций
  - ⏱ Ожидание по времени
  - 🎨 Ожидание цвета пикселя
  - 🔄 Ожидание изменения пикселя
  - 🖼 Ожидание изображения на экране
  - 📝 Ожидание текста (OCR)
  - ❓ Условные действия
  - 🔁 Циклы
  - 📸 Скриншот области
  - 📋 Логирование

- **Экспорт в AHK** — генерация готовых AutoHotkey скриптов
- **Интеграция AHK v2** — парсинг, валидация и выполнение (`native/emulated`)
- **Сохранение/загрузка** — JSON формат для task-досок
- **Тёмная тема** — удобный интерфейс

## 🚀 Установка

```bash
# Активация виртуального окружения
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt
```

## 📖 Использование

### Запуск GUI

```bash
python main.py
```

### CLI демонстрация

```bash
python main.py --cli
```

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `F5` | Запуск task-доски |
| `Shift+F5` | Остановка выполнения |
| `F9` | Начать/остановить запись |
| `Ctrl+N` | Новая доска |
| `Ctrl+O` | Открыть доску |
| `Ctrl+S` | Сохранить доску |

## 🏗 Архитектура

```
AHK_Manipulator_app/
├── main.py              # Ядро: модели данных, исполнитель, контроллеры
├── requirements.txt     # Зависимости
├── ui/                  # GUI модуль
│   ├── __init__.py
│   ├── main_window.py   # Главное окно
│   ├── task_board_widget.py  # Виджет task-доски
│   ├── action_panel.py  # Панель добавления действий
│   ├── properties_panel.py  # Панель свойств
│   └── screen_overlay.py  # Экранный оверлей
└── README.md
```

### Компоненты ядра

| Класс | Описание |
|-------|----------|
| `BackendApplication` | Главный сервис приложения |
| `TaskBoard` | Task-доска (контейнер строк) |
| `TaskRow` | Строка доски (контейнер действий) |
| `Action` | Действие с параметрами |
| `ExecutionEngine` | Исполнитель действий (async) |
| `ScreenCapture` | Захват экрана и работа с пикселями |
| `MouseController` | Управление мышью |
| `KeyboardController` | Управление клавиатурой |

## 📝 Пример экспорта в AHK

```ahk
; AHK Script generated by AHK Manipulator
; Board: Тестовая доска

; Row: Действие 1
Click, left, 960, 540
Sleep, 1000
MouseMove, 100, 200
Send, {Enter}
```

## 🔧 Настройка

Конфигурация в `main.py`:

```python
CONFIG = {
    "app_name": "AHK Manipulator",
    "version": "0.1.0",
    "default_delay_ms": 1000,
    "pixel_check_interval_ms": 100,
    "mouse_poll_interval_ms": 50,
}
```

## 🛠 Расширение

### Добавление нового типа действий

1. Добавьте тип в `ActionType` enum:
```python
class ActionType(Enum):
    MY_NEW_ACTION = auto()
```

2. Реализуйте метод в `ActionExecutor`:
```python
async def _my_new_action(self, action: Action) -> None:
    # Логика действия
```

3. Добавьте обработку в `execute_action`

4. Добавьте иконку и название в UI компоненты

## ⚠️ Ограничения

- macOS: требуются разрешения на доступность (Accessibility)
- Некоторые функции (OCR, поиск изображений) в разработке
- AHK скрипты генерируются для Windows (AutoHotkey)

## AHK v2

- Документация: `AHK_V2_INTEGRATION.md`
- Пример: `examples/ahk_v2_sample.ahk`

## 📄 Лицензия

MIT
